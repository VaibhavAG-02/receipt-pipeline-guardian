# Everything here is OSI-licensed and runs locally. No managed service, no
# account, no spend. Pinned to majors to keep CI reproducible.
pandas>=2.1,<4
numpy>=1.26,<3
pyarrow>=15
duckdb>=1.0,<2
dbt-core>=1.8,<2
dbt-duckdb>=1.8,<2
scikit-learn>=1.4,<2
xgboost>=2.0,<4
shap>=0.44,<1
streamlit>=1.34,<2

# OCR (photo-upload tab). The Tesseract *binary* is installed separately via
# packages.txt -- Streamlit Cloud reads that for apt packages. Kept here rather
# than in requirements-ocr.txt because Cloud installs only requirements.txt, so
# an optional-extras file would leave the deployed photo path broken.
# RapidOCR: PaddleOCR models on ONNX -- a learned text detector that handles
# skewed/curled/shadowed phone photos far better than Tesseract, still free and
# offline. Tesseract stays as a fallback.
rapidocr-onnxruntime>=1.3,<2
pytesseract>=0.3.10
Pillow>=10
