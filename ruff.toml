line-length = 100
target-version = "py310"

[lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]

[lint.per-file-ignores]
"app/streamlit_app.py" = ["E402"]  # sys.path must be set before importing rpg
